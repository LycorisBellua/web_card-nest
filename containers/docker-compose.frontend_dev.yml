services:
  app:
    build:
      target: frontend_dev
    ports:
      - "3000:3000"
      - "5173:5173"
    environment:
      NODE_ENV: development
    volumes:
      - ../backend:/app/backend
      - ../frontend:/app/frontend