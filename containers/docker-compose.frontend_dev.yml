services:
  app:
    build:
      target: frontend_dev
    ports:
      - "3000:3000"
      - "5173:5173"
      - "5555:5555"
    environment:
      NODE_ENV: development
    volumes:
      - ../backend:/app/backend
      - ../frontend:/app/frontend
