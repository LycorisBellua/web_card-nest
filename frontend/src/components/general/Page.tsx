import styled from 'styled-components';

const Page = styled.div`
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  position: relative;
`;

export default Page;
